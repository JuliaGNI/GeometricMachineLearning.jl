name: PDF

on:
  push:
    branches:
      - main
      - make_mac_docs
      - volume_preserving_transformer
  pull_request:

jobs:
  LatexDocsUbuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          sudo apt-get install imagemagick
          sudo apt-get install poppler-utils
          wget https://mirror.ctan.org/systems/texlive/tlnet/install-tl-unx.tar.gz
          zcat < install-tl-unx.tar.gz | tar xf -
          rm install-tl-unx.tar.gz
          cd install-tl-*
          sudo perl ./install-tl --no-interaction
          cd ..
          echo "${HOME}/local/texlive/2024/bin/x86_64-linux" >> $GITHUB_PATH
          tlmgr init-usertree
          tlmgr install preamble
      - run: |
          make linux -C docs/src/tikz
      - uses: julia-actions/setup-julia@latest
        with:
          version: '1'
      - run: |
          cd docs
          make install_brenier_two_fluid test_docs
          cd .. 
      - run: julia --project=docs --threads=2 docs/make.jl latex_output_no_pdf
      - run: xelatex -shell-escape docs/build/GeometricMachineLearning.jl.tex
      - name: Upload PDF file
        uses: actions/upload-artifact@v4
        with:
          name: Ubuntu-PDF
          path: GeometricMachineLearning.jl.pdf
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  LatexDocsMac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: | 
          curl https://mirror.ctan.org/systems/texlive/tlnet/install-tl-unx.tar.gz 
          zcat < install-tl-unx.tar.gz | tar xf -
          rm install-tl-unx.tar.gz
          cd install-tl-*
          perl ./install-tl --no-interaction
          cd ..
          ls ${HOME}/local/texlive/2024/bin
          echo "${HOME}/local/texlive/2024/bin/x86_64-macos" >> $GITHUB_PATH
          tlmgr init-usertree
          tlmgr install preamble
      - run: |
          make mac -C docs/src/tikz
      - uses: julia-actions/setup-julia@latest
        with:
          version: '1'
      - run: |
          cd docs
          make install_brenier_two_fluid test_docs
          cd .. 
      - run: julia --project=docs --threads=2 docs/make.jl latex_output_no_pdf
      - run: xelatex -shell-escape docs/build/GeometricMachineLearning.jl.tex
      - name: Upload PDF file
        uses: actions/upload-artifact@v4
        with:
          name: Mac-PDF
          path: GeometricMachineLearning.jl.pdf
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}